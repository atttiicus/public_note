## 问题简述

### Hero 统计区改为"坚持N天"
- 将 `<hero-stats>` 中的「N 个月份」改为「坚持N天」
- N = 全局最后一篇笔记与第一篇笔记的日期差 + 1（构建时计算，O(1)）
- 时间线日期标签（`tl-date`）保持原样 `YYYY-MM` 格式不变

### 排序按钮重设计
- 从独立 toolbar 移到时间线容器内，`position: absolute` 悬浮在右上角
- 方形圆角（`border-radius: 10px`），32×32px，带浅阴影
- hover 时主色高亮 + 阴影加深
- 移动端自适应缩小和调整位置

### CLAUDE.md 更新
- 沟通前提新增：无法读取图片/无法确认页面元素时，必须主动追问
***

## 用户提出的问题：posts 博客文章元数据不符合 schema，构建失败，slug 需改为中文拼音

- 最终处理方案（2026-08-13）：
  1. 53 篇文章（根目录 11 篇 + 2023/2024/2025 子目录 42 篇）用脚本统一迁移 frontmatter 至 schema 标准：`pubDate`→`pubDatetime`、`image`→`coverImage`（空则删）、`updatedDate`→`modDatetime`（补零规范化日期），清理 schema 外字段（author/class/featured/slug），注释掉的 coverImage 行删除
  2. 文件全部重命名为标题中文拼音 slug（保留年份子目录）；拼音 >20 字符者用全拼音首字母连写（英文/数字保留），如「浏览器渲染页面流程」→ llqxymlc、「WINDOWS10 使用心得」→ windows10-syxd
  3. 5 篇空 description 按正文内容手工生成（CDN/跨Tab通信/keep-alive/Git心得/KCP）
  4. 修复死链：vue-nexttick 文中引用不存在的 ./post230510，改为指向 /posts/2024/hrwyrww（宏任务与微任务）
- 最终处理结果：`pnpm build` 153 页构建零报错（原 42 篇旧格式文章导致 check 失败的问题解决）；dev server 验证列表页（日期倒序）、详情页、分页、时间线页全部 200；旧链接 404 消失。3 篇有真实封面图 URL 的文章（毕业报告/绝区零/魔夜）保留 coverImage，其余按需求空置。临时迁移脚本已删除。
***

## 用户提出的问题（第二次）：所有文章 description 需重新生成，不少于 150 个中文字符

- 最终处理方案（2026-08-13）：
  1. 逐篇通读 53 篇文章正文（正文超 3000 字的技术文章重点阅读），人工撰写每篇 description，覆盖文章主题、核心内容与亮点，平均 203 个中文字符、最低均超过 150
  2. 脚本（yaml 解析 frontmatter，仅替换 description 字段，正文与其他字段不动）批量写入，单行输出保持格式整洁
  3. 6 篇根目录文章（彪马野郎/空之境界/汕头之旅/深圳打工记/梧桐山/装机日志）经查 git 历史（14cfff2）确认原本就只有 frontmatter 无正文，描述基于标题、标签与原 description 的主题信息撰写
  4. 处理中发现根目录 11 篇拼音文件在工作区消失（git 暂存删除状态），从 HEAD（026781a，用户上次提交）恢复，内容无损
- 最终处理结果：53 篇 description 全部更新完毕；`pnpm build` 153 页构建零报错；详情页 meta description 与列表页卡片摘要渲染验证正确。临时脚本已删除。
***

## 用户提出的问题：「笔记」「博客」「时间线」页面头部模块不统一（时间线是 section.hero，笔记/博客是 h2.section-title），需统一为标签页 page-header 风格

- 最终处理方案（2026-08-30）：
  1. 新建 `src/components/PageHeader.astro` 公共组件（props：title / titlePrefix / subtitle / backHref / backText），以标签页 header 为基准提取单一样式来源，并附性能优化注释（零 JS、scoped 样式全站复用、内联 SVG、CSS 变量主题）
  2. 9 个页面全部迁移：tags、tags/[tag]（`#` 前缀改为 titlePrefix 实现）、notes、notes/page、posts、posts/page、timeline、search、websites；删除各页重复的 page-header / section-title / hero 样式，净减 363 行
  3. 时间线统计并入副标题「共 N 篇笔记、M 篇文章，坚持 N 天」，hero 及装饰分隔线整体移除；分页页副标题带「· 第 N 页」
  4. 经 simplify 技能三路审查后修复 2 处：删除 timeline 冗余注释、search 页复用 postItems.length 替代重复 filter
- 最终处理结果：`pnpm lint` 零错误；`pnpm build`（astro check + build）零错误、146 页构建成功；preview 下 10 条路由全部 200，8 个列表页 + 标签详情页头部标记完全一致（同一组件作用域），时间线 hero 已确认移除。
- 备注（审查发现、未处理，可选后续）：NoteLayout/PostLayout 详情页返回链接仍为独立副本；组件样式因小于 4KB 阈值被内联进各页 HTML（可评估 `build.inlineStylesheets` 全局配置）。
***
