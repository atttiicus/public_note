## 问题简述

### 首页 + 时间线页排序改为从新到旧
- **首页 (`utils/notes.ts`)**：`buildRenderItems()` 排序从标题（zh-CN）改为按 `date` 降序，无日期排末尾，同日按标题排。首页、分页、标签页均受影响。
- **时间线 (`timeline.astro`)**：默认排序从升序改为降序（最新月份在前）。

### 时间线排序切换按钮
- 新增 `TimelineSortToggle.tsx` React island（`client:load`）：点击切换排序方向，通过反转 `.tl-row` DOM 子节点顺序实现（O(n) 单次遍历），无需重渲染。
- 按钮显示"最新在前"/"最早在前"，带排序图标动画提示。
- 构建 70 页无错误，排序正确，按钮渲染正常。
***
